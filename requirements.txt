--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML/NLP
numpy==1.26.4          # Current stable (faster, better memory management)
pandas==2.1.4          # Modern version with better performance
scikit-learn==1.3.2     # Current stable (with many optimizations)
nltk==3.8.1            # Updated NLP toolkit
matplotlib

# FastAPI Stack
fastapi==0.109.1        # Modern FastAPI version
uvicorn==0.27.0         # Matching ASGI server
pydantic==2.6.1         # Major upgrade (faster validation)
python-jose==3.3.0      # Kept same (still compatible)
python-multipart==0.0.9  # Updated form handling

# Monitoring
prometheus-client==0.19.0  # Updated metrics library
prometheus-fastapi-instrumentator==5.11.1  # Instrumentation for FastAPI metrics

# Additional recommended for ML serving:
joblib==1.3.2          # Better model serialization
typing-extensions==4.9.0  # Better type hints support
anyio==4.2.0           # Improved async compatibility

# Deep learning and Hugging Face support
torch==2.2.2+cpu     # For deep learning model operations
transformers         # For Hugging Face model and tokenizer support
huggingface_hub      # For downloading models from Hugging Face Hub
openpyxl             # For reading Excel files with pandas
